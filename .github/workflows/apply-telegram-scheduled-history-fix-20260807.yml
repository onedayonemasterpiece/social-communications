name: Apply Telegram scheduled-history fix 2026-08-07

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-telegram-scheduled-history-fix-20260807.yml

permissions:
  contents: write

concurrency:
  group: apply-telegram-scheduled-history-fix-20260807
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply fix
        run: python scripts/apply_telegram_scheduled_history_fix_20260807.py

      - name: Run contract checks
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check 'telethon==1.40.0' 'requests==2.32.5'
          python -m py_compile scripts/social_registry.py scripts/telegram_publish.py scripts/vk_publish.py
          python -m unittest discover -s tests -p 'test_*.py' -v

      - name: Commit non-workflow fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name onedayonemasterpiece
          git config user.email estershow6@gmail.com
          git add scripts/telegram_publish.py tests/test_social_publish_contracts.py
          git diff --cached --quiet && { echo 'No changes to commit'; exit 0; }
          git commit -m 'Use raw Telegram scheduled-history RPC for verification'
          git push origin HEAD:main
